import { FirebaseUser } from '../types';

// Os cadastros reais vivem somente no Firebase. Manter esta lista vazia evita
// publicar usuários e senhas junto com o código-fonte.
export const INITIAL_USERS: FirebaseUser[] = [];
